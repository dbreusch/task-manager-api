module.exports = {
    setApiKey() {
        // No API keys will be set
    },
    async send() {
        // No emails will be sent
        // "async" is needed because I use Promise from normal send in account.js
    }
}